const width = 1360;
const height = 640;
const boardX = width - 955;
const boardY = height - 595;
const size = 10;
const cellSize = 55;

const config = {
  width,
  height,
  boardX,
  boardY,
  size,
  cellSize,
};

export default config;
